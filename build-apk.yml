name: Build APK

# Bu fayl loyihani GitHubga yuklaganingizda ishga tushadi va
# Android Studio o'rnatmasdan ham tayyor .apk faylni avtomatik yasab beradi.
# Natijani: repo -> Actions -> oxirgi run -> Artifacts -> "allsms-sender-debug-apk"
# bo'limidan yuklab olasiz.

on:
  push:
    branches: [ main, master ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: JDK 17 o'rnatish
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle wrapper huquqi
        run: chmod +x ./gradlew

      - name: Debug APK yasash
        run: ./gradlew assembleDebug --no-daemon

      - name: APK ni artifact sifatida saqlash
        uses: actions/upload-artifact@v4
        with:
          name: allsms-sender-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
